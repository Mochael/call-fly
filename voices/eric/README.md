For the optional legacy Qwen pipeline, supply your own authorized voice sample as `reference.wav` (mono, 24 kHz) and its exact transcript as `transcript.txt`. Personal files in this directory are excluded from Git. No recording or transcript is distributed with the repository. See [the legacy setup](../../docs/qwen-voice-legacy.md).
